import { RequestHandler } from 'express';

export const createActor: RequestHandler = (req, res, next): void => {};
export const deleteActor: RequestHandler = (req, res, next): void => {};
export const updateActor: RequestHandler = (req, res, next): void => {};
