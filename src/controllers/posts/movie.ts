import { RequestHandler } from 'express';

export const createMovie: RequestHandler = (req, res, next): void => {};
export const deleteMovie: RequestHandler = (req, res, next): void => {};
export const updateMovie: RequestHandler = (req, res, next): void => {};
